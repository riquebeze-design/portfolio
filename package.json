{
  "name": "whispering-koala-hop-root",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "cd frontend && pnpm dev",
    "install-all": "pnpm install",
    "build-all": "pnpm --filter frontend build && pnpm --filter backend build",
    "start-frontend": "pnpm --filter frontend dev",
    "start-backend": "pnpm --filter backend dev"
  },
  "dependencies": {
    "embla-carousel-react": "^8.6.0",
    "slugify": "^1.6.6",
    "three": "^0.182.0"
  },
  "devDependencies": {
    
  }
}